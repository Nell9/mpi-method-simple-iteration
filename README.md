# ⚡ Parallel Numerical Methods: Simple Iteration with MPI

![C++](https://img.shields.io)
![MPI](https://img.shields.io)

Параллельная реализация **метода простой итерации** (метод Якоби) на языке C++ с использованием технологии **MPI** для высокопроизводительных вычислений.

---

## 📖 Описание проекта

Проект предназначен для решения систем линейных алгебраических уравнений (СЛАУ) вида $Ax = b$ в распределенных вычислительных системах. Основная идея заключается в разделении строк матрицы коэффициентов между доступными процессами для ускорения итерационного процесса.

### Параллельная логика:
- **Распределение данных:** Матрица $A$ и вектор $b$ разбиваются на части («полосы») и распределяются между MPI-процессами.
- **Итерационный цикл:** Каждый процесс вычисляет свою часть вектора $x^{(k+1)}$ на основе полного вектора $x^{(k)}$ с предыдущего шага.
- **Синхронизация:** Обмен результатами между процессами через `MPI_Allgather` на каждой итерации.
- **Критерий остановки:** Вычисление глобальной нормы погрешности через `MPI_Allreduce`.

---

## ✨ Ключевые особенности

- **Масштабируемость:** Эффективная работа как на локальных многоядерных процессорах, так и на кластерных системах.
- **Оптимизация обменов:** Минимизация накладных расходов на пересылку данных между узлами.
- **Работа с большими данными:** Возможность решения систем размерности, превышающей объем памяти одного узла.

---

## 🛠 Технологический стек

*   **Язык:** C++17
*   **Интерфейс:** MPI (OpenMPI / MPICH / MS-MPI)
*   **Сборка:** CMake или Makefile

---

## 🚀 Сборка и запуск

1. **Установите библиотеку MPI** (например, OpenMPI):
   ```bash
   sudo apt-get install openmpi-bin libopenmpi-dev
